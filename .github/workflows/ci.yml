name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm check
      - run: pnpm build

  backend:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          components: clippy, rustfmt

      - name: Install Linux deps
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            pkg-config \
            libgtk-3-dev \
            libasound2-dev \
            libxdo-dev \
            libxtst-dev \
            libxi-dev \
            libx11-dev

      # CI doesn't need real whisper-cli binaries to run unit/integration
      # tests — Tauri's build.rs just validates that `externalBin` and
      # `resources` paths exist (and that globs match at least one file).
      # We create empty placeholders so those checks pass:
      #   * one sidecar `.exe` matching the target triple → externalBin
      #   * one `.dll` file → satisfies the `binaries/*.dll` resource glob
      # Note: the DLL glob in tauri.conf.json is global (not Windows-only),
      # so the placeholder DLL has to exist on every CI OS. Local dev uses
      # scripts/download-whisper-cli.* for the real binaries.
      - name: Stub sidecar + DLL placeholders
        shell: bash
        run: |
          mkdir -p src-tauri/binaries
          touch "src-tauri/binaries/ci-placeholder.dll"
          case "${{ matrix.os }}" in
            windows-latest)
              touch "src-tauri/binaries/whisper-cli-x86_64-pc-windows-msvc.exe"
              ;;
            macos-latest)
              touch "src-tauri/binaries/whisper-cli-aarch64-apple-darwin"
              chmod +x "src-tauri/binaries/whisper-cli-aarch64-apple-darwin"
              ;;
            ubuntu-latest)
              touch "src-tauri/binaries/whisper-cli-x86_64-unknown-linux-gnu"
              chmod +x "src-tauri/binaries/whisper-cli-x86_64-unknown-linux-gnu"
              ;;
          esac

      - name: cargo fmt --check
        working-directory: src-tauri
        run: cargo fmt --check

      - name: cargo clippy
        working-directory: src-tauri
        run: cargo clippy -- -D warnings

      - name: cargo test
        working-directory: src-tauri
        run: cargo test
